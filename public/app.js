const starterImages = [
  {
    imageUrl: 'http://labs-assets.bria.ai/photorealistic__high_resolution_rendering__balloon_of_lettering___HAPPY_NEW_YEAR.png',
    suggestedPrompt: 'photorealistic, high resolution rendering, balloon of lettering, "HAPPY NEW YEAR 2026", used color is Pantone Bran, Crocus, Deja vu blue, perfect composition and alignment, natural light set, white backdrop, ultra realistic, rich detail',
		structured_prompt: {
			"short_description": "A photorealistic, high-resolution rendering of balloon lettering spelling out \"HAPPY NEW YEAR 2026\" against a pristine white backdrop. The balloons are meticulously arranged with perfect composition and alignment, showcasing a vibrant color palette of Pantone Bran, Crocus, and Deja Vu Blue. Natural light illuminates the scene, creating subtle, soft shadows that enhance the ultra-realistic and rich detail of each balloon, emphasizing their smooth, reflective surfaces and volumetric presence.",
			"objects": [
			{
			"description": "A cluster of individual foil balloons, each meticulously shaped to form a letter or number. The balloons are inflated to perfection, showing no creases or imperfections, and possess a slight sheen that reflects the ambient light. Each character is distinct and perfectly formed, contributing to the overall message.",
			"location": "center",
			"relationship": "The balloons are arranged to form the phrase \"HAPPY NEW YEAR 2026\", with each character positioned in a visually balanced and harmonious relationship to its neighbors.",
			"relative_size": "large within frame",
			"shape_and_color": "Varied letter and number shapes, colored in a vibrant mix of Pantone Bran (a rich, earthy brown), Crocus (a bright, cheerful purple), and Deja Vu Blue (a deep, calming blue).",
			"texture": "Smooth, reflective, metallic foil.",
			"appearance_details": "Each balloon exhibits subtle volumetric depth and a slight curvature, giving them a realistic, inflated appearance. The edges are crisp and well-defined.",
			"orientation": "Upright and horizontally aligned, forming the phrase."
			}
			],
			"background_setting": "A seamless, pristine white studio backdrop that extends infinitely, creating a clean and minimalist environment. The white surface is perfectly smooth and free of any distractions, allowing the balloon lettering to be the sole focal point.",
			"lighting": {
			"conditions": "Natural light, soft and diffused.",
			"direction": "Evenly distributed from above and slightly to the front, mimicking a large softbox setup.",
			"shadows": "Soft, subtle, and elongated shadows cast directly beneath and behind the balloons, indicating their three-dimensional form and adding depth without being harsh."
			},
			"aesthetics": {
			"composition": "Centered and perfectly aligned, utilizing principles of visual balance and symmetry for a harmonious and pleasing arrangement.",
			"color_scheme": "A curated palette of Pantone Bran, Crocus, and Deja Vu Blue, creating a sophisticated yet celebratory contrast against the white background.",
			"mood_atmosphere": "Celebratory, elegant, clean, and optimistic.",
			"preference_score": "very high",
			"aesthetic_score": "very high"
			},
			"photographic_characteristics": {
			"depth_of_field": "Shallow, with the balloons in sharp focus and the background subtly blurred to emphasize the subject.",
			"focus": "Sharp focus on the balloon lettering, capturing every minute detail of their surface and form.",
			"camera_angle": "Eye-level, providing a direct and engaging view of the celebratory message.",
			"lens_focal_length": "Standard to slightly telephoto, to minimize distortion and present the balloons accurately."
			},
			"style_medium": "3D render",
			"text_render": [
			{
			"text": "HAPPY NEW YEAR 2026",
			"location": "center",
			"size": "large within frame",
			"color": "Pantone Bran, Crocus, Deja Vu Blue (alternating per word/number)",
			"font": "Bold, sans-serif, balloon-style typeface",
			"appearance_details": "Each character is a distinct, inflated foil balloon, with a smooth, reflective surface."
			}
			],
			"context": "This is a concept for a high-resolution 3D render, ideal for a New Year's greeting card, digital banner, or a festive promotional image, emphasizing elegance and modern celebration.",
			"artistic_style": "ultra realistic, clean, vibrant"
		}
  },
  {
    imageUrl: 'http://labs-assets.bria.ai/A_cozy_rooftop_garden_in_Paris_overlooking_Paris__rustic_wooden_decking__woven_r.png',
    suggestedPrompt: 'A cozy rooftop garden in Paris under the warm glow of lights at night , overlooking Paris, rustic wooden decking, woven rattan furniture, lush climbing vines and potted olive trees, long shadows dancing across linen fabrics, tranquil ambiance blending urban charm with organic serenity, effortlessly chic Parisian vibe. brown orange and blue hues',
		structured_prompt: {
			"short_description": "An enchanting rooftop garden in Paris, bathed in the warm glow of late afternoon, offers a serene escape with panoramic views of the city. Rustic wooden decking forms the foundation, adorned with comfortable woven rattan furniture. Lush green climbing vines cascade gracefully, while potted olive trees add a touch of Mediterranean charm. Long, dancing shadows stretch across soft linen fabrics draped over furniture, enhancing the tranquil ambiance. The scene effortlessly blends urban sophistication with organic serenity, capturing an authentically chic Parisian vibe through a palette of warm browns, oranges, and subtle blues.",
			"objects": [
			{
			"description": "A section of rustic wooden decking, weathered to a soft grey-brown, providing a natural and inviting surface for the garden. The planks are arranged horizontally, creating a sense of breadth and stability.",
			"location": "bottom foreground, extending to mid-ground",
			"relationship": "Forms the primary ground surface, supporting all other objects.",
			"relative_size": "large within frame",
			"shape_and_color": "Rectangular planks, weathered grey-brown",
			"texture": "Rough, textured wood grain with visible imperfections",
			"appearance_details": "Some planks show subtle variations in color and texture, suggesting age and exposure to elements.",
			"orientation": "horizontal"
			},
			{
			"description": "A comfortable, low-slung armchair crafted from intricately woven rattan, featuring a natural, light brown hue. It is adorned with soft, off-white linen cushions, inviting relaxation.",
			"location": "center-left mid-ground",
			"relationship": "Provides seating within the garden, positioned to face the city view.",
			"relative_size": "medium",
			"shape_and_color": "Curved, organic shape, natural light brown rattan with off-white cushions",
			"texture": "Woven, slightly rough rattan; smooth, soft linen",
			"appearance_details": "The rattan weave creates interesting patterns of light and shadow. The linen cushions appear slightly rumpled, suggesting recent use.",
			"orientation": "facing slightly right"
			},
			{
			"description": "A cluster of lush green climbing vines, possibly ivy or wisteria, gracefully ascending a nearby wall or trellis. Their leaves are vibrant and varied in shade, creating a dense, organic curtain.",
			"location": "left background, extending upwards",
			"relationship": "Provides a natural, living backdrop and adds vertical interest to the garden.",
			"relative_size": "large",
			"shape_and_color": "Irregular, organic shapes, various shades of green",
			"texture": "Leafy, slightly glossy",
			"appearance_details": "Some leaves catch the light, highlighting their intricate venation. Small, delicate tendrils are visible.",
			"number_of_objects": 1,
			"orientation": "vertical, climbing"
			},
			{
			"description": "A terracotta pot containing a small, elegant olive tree. The tree has slender branches with silvery-green leaves and a gnarled, rustic trunk. The pot is a classic, unglazed terracotta with a slightly aged patina.",
			"location": "right mid-ground",
			"relationship": "Adds a touch of Mediterranean flora and acts as a natural divider within the space.",
			"relative_size": "medium",
			"shape_and_color": "Conical tree shape, silvery-green leaves, brown trunk; round, orange-brown pot",
			"texture": "Smooth leaves, rough bark, porous terracotta",
			"appearance_details": "The pot shows subtle signs of wear, with some moss or mineral deposits. The olive leaves shimmer slightly in the light.",
			"orientation": "upright"
			},
			{
			"description": "A soft, off-white linen throw draped casually over the back of the rattan armchair, with its folds catching the light and shadow. Its texture is visibly soft and inviting.",
			"location": "center-left mid-ground, on the armchair",
			"relationship": "Adds comfort and a touch of elegance to the seating area, also serving as a surface for shadows.",
			"relative_size": "small",
			"shape_and_color": "Irregular, flowing shape, off-white",
			"texture": "Soft, slightly wrinkled linen",
			"appearance_details": "The fabric has a natural, slightly slubby texture. Long, dark shadows from unseen elements fall across its surface.",
			"orientation": "draped"
			}
			],
			"background_setting": "The background reveals a breathtaking panoramic view of the Parisian skyline, with iconic Haussmannian buildings stretching into the distance. The Eiffel Tower is subtly visible in the far distance, partially obscured by other rooftops, adding to the authentic urban charm. The sky is a soft, clear blue, transitioning to warm orange and pink hues near the horizon, indicative of a late afternoon or early evening.",
			"lighting": {
			"conditions": "Golden hour, late afternoon sunlight",
			"direction": "Side-lit from the right, with light raking across surfaces",
			"shadows": "Long, soft, and distinct shadows cast by the furniture, plants, and unseen architectural elements, dancing across the decking and linen fabrics, creating depth and visual interest."
			},
			"aesthetics": {
			"composition": "Rule of thirds, with the garden elements occupying the foreground and mid-ground, and the Parisian skyline filling the upper third. Leading lines from the decking draw the eye towards the view.",
			"color_scheme": "Warm complementary colors, dominated by earthy browns and oranges from the wood and terracotta, contrasted with lush greens of the plants and soft blues of the sky and distant city.",
			"mood_atmosphere": "Tranquil, serene, sophisticated, cozy, and effortlessly chic.",
			"preference_score": "very high",
			"aesthetic_score": "very high"
			},
			"photographic_characteristics": {
			"depth_of_field": "Shallow, with the immediate garden elements in sharp focus and the Parisian skyline gently blurred in the background.",
			"focus": "Sharp focus on the rattan furniture and foreground plants.",
			"camera_angle": "Eye-level, as if sitting in the garden, looking out.",
			"lens_focal_length": "Standard to slightly wide-angle, capturing both the intimacy of the garden and the expansive view."
			},
			"style_medium": "photograph",
			"context": "This is a concept for a lifestyle photograph, possibly for a travel magazine, interior design blog, or a high-end real estate listing, showcasing an idyllic urban oasis."
		}
  },
  {
    imageUrl: 'http://labs-assets.bria.ai/hyper_realistic_ultra_detailed_digital_illustration_back_view_of_an_astronaut_wi.png',
    suggestedPrompt: 'hyper realistic ultra detailed digital illustration back-view of an astronaut with an orange suit standing on a cliff of an alien planet overlooking a majestic martian landscape',
		structured_prompt: {
			"short_description": "A hyper-realistic, ultra-detailed digital illustration from a back-view perspective, featuring an astronaut in a vibrant orange suit standing on the rugged cliff edge of an alien planet. The astronaut gazes out over a breathtaking Martian-like landscape, characterized by vast canyons, towering rock formations, and a distant, majestic celestial body. The scene is bathed in warm, golden hues, emphasizing the desolate beauty and grandeur of the extraterrestrial environment. The composition highlights the solitary figure against the immense alien vista, evoking a sense of wonder and exploration.",
			"objects": [
			{
			"description": "A solitary astronaut, viewed from the back, standing confidently at the edge of a cliff. The suit is highly detailed, showing intricate seams, reflective visor on the helmet, life support systems on the back, and various utility pouches. The material appears durable and slightly textured, designed for extreme environments.",
			"location": "center-left foreground",
			"relationship": "The astronaut is the primary subject, positioned to observe the vast alien landscape before them.",
			"relative_size": "large within frame",
			"shape_and_color": "Humanoid shape, predominantly bright orange with white and grey accents.",
			"texture": "Durable fabric, metallic, reflective visor.",
			"appearance_details": "The helmet's visor reflects a distorted image of the alien sky and the distant planet, adding depth and intrigue. Small lights on the suit's back and arms are subtly illuminated.",
			"pose": "Standing upright, slightly leaning forward, with arms relaxed at their sides, head tilted slightly upwards to take in the view.",
			"clothing": "Full-body advanced space suit, bright orange with white and grey panels, integrated life support backpack.",
			"action": "Observing the landscape.",
			"gender": "Androgynous, obscured by the suit.",
			"orientation": "Facing away from the viewer, towards the distant landscape."
			},
			{
			"description": "The cliff edge where the astronaut stands, composed of jagged, weathered rock formations. The surface is uneven, showing signs of erosion from alien winds and possibly ancient water flows. The colors range from deep reds and oranges to ochre and brown, consistent with a Martian environment.",
			"location": "bottom foreground",
			"relationship": "Serves as the immediate ground for the astronaut and the transition point to the expansive landscape.",
			"relative_size": "large within frame",
			"shape_and_color": "Irregular, angular rock formations, predominantly reddish-brown and orange.",
			"texture": "Rough, craggy, weathered rock.",
			"appearance_details": "Small, scattered alien pebbles and dust are visible on the surface, catching the warm light.",
			"orientation": "Horizontal, extending across the bottom of the frame and receding into the mid-ground."
			},
			{
			"description": "A majestic alien planet, resembling Mars, dominating the distant horizon. It features vast, deep canyons, towering mesas, and undulating plains, all rendered in a palette of warm reds, oranges, and earthy browns. Atmospheric haze softens the distant features, creating a sense of immense scale.",
			"location": "mid-ground to background",
			"relationship": "The primary subject of the astronaut's gaze and the defining element of the alien setting.",
			"relative_size": "large within frame",
			"shape_and_color": "Expansive, rugged planetary surface, predominantly red, orange, and brown.",
			"texture": "Rocky, dusty, with visible geological strata.",
			"appearance_details": "Subtle atmospheric effects, such as dust devils or distant cloud formations, could be hinted at. The scale is immense, with features stretching to the horizon.",
			"orientation": "Spanning the entire mid-ground and background, receding into the distance."
			}
			],
			"background_setting": "The background is an expansive, desolate alien landscape, reminiscent of Mars. It features a vast, reddish-orange sky with a faint, hazy atmosphere. Distant, colossal rock formations and deep canyons stretch as far as the eye can see, bathed in the warm glow of a setting or rising alien sun. The horizon is curved, hinting at the planetary scale. There are no signs of life or vegetation, emphasizing the stark, otherworldly beauty.",
			"lighting": {
			"conditions": "Golden hour, with a warm, low-angle light source.",
			"direction": "Side-lit from the right, casting long shadows.",
			"shadows": "Long, soft, and well-defined shadows stretching across the alien terrain, adding depth and drama to the landscape."
			},
			"aesthetics": {
			"composition": "Rule of thirds, with the astronaut positioned on the left vertical third line and the horizon on the upper horizontal third line, creating a sense of balance and leading the eye towards the vast landscape.",
			"color_scheme": "Warm complementary colors, dominated by oranges, reds, and browns, with subtle hints of deep blues and purples in the distant sky.",
			"mood_atmosphere": "Awe-inspiring, solitary, adventurous, and majestic.",
			"preference_score": "very high",
			"aesthetic_score": "very high"
			},
			"photographic_characteristics": {
			"depth_of_field": "Deep, with both the astronaut and the distant landscape in sharp focus to emphasize the grandeur of the scene.",
			"focus": "Sharp focus on subject and background.",
			"camera_angle": "Slightly low angle, looking up at the astronaut and out over the landscape, enhancing the sense of scale and majesty.",
			"lens_focal_length": "Wide-angle, to capture the expansive vista and the astronaut within it."
			},
			"style_medium": "digital illustration",
			"context": "This is a concept for a hyper-realistic science fiction illustration, potentially for a book cover, game art, or a promotional image for space exploration.",
			"artistic_style": "hyper-realistic, detailed, epic"
		}
  },
  {
    imageUrl: 'http://labs-assets.bria.ai/A_hyper_detailed__ultra_fluffy_owl_sitting_in_the_trees_at_night__looking_direct.png',
    suggestedPrompt: 'A hyper-detailed, ultra-fluffy owl sitting in the trees at night, looking directly at the camera with wide, adorable, expressive eyes. Its feathers are soft and voluminous, catching the cool moonlight with subtle silver highlights. The owl\'s gaze is curious and full of charm, giving it a whimsical, storybook-like personality.',
		structured_prompt: {
			"short_description": "A hyper-detailed, ultra-fluffy owl is perched on a tree branch at night, its voluminous feathers catching the cool moonlight with subtle silver highlights. The owl looks directly at the viewer with wide, adorable, expressive eyes, conveying a curious and charming gaze. The scene is set against a dark, mystical forest background, emphasizing the owl's whimsical, storybook-like personality. The overall image evokes a sense of wonder and enchantment, with intricate details on the owl's plumage and the surrounding nocturnal environment.",
			"objects": [
			{
			"description": "A hyper-detailed, ultra-fluffy owl with soft, voluminous feathers. Its plumage is a mix of muted grays, browns, and whites, with individual feathers clearly defined and appearing incredibly soft to the touch. Subtle silver highlights shimmer on the edges of its feathers, indicating the cool moonlight reflecting off them. Its body is plump and rounded, giving it an endearing appearance.",
			"location": "center",
			"relationship": "The central subject of the image, perched securely on a tree branch.",
			"relative_size": "large within frame",
			"shape_and_color": "Rounded, fluffy body with a mix of earthy tones and silver highlights.",
			"texture": "ultra-fluffy, soft, feathery",
			"appearance_details": "Each feather is distinct, showing incredible detail and depth. The overall impression is one of extreme softness and volume.",
			"expression": "Wide, adorable, expressive eyes that are dark and deep, conveying curiosity and charm. Its gaze is direct and engaging, almost as if it's inviting interaction.",
			"action": "Perched calmly on a branch, looking directly forward.",
			"orientation": "Facing directly forward, head slightly tilted."
			}
			],
			"background_setting": "A dark, mystical forest at night. Silhouetted tree trunks and branches are visible in the deep background, creating a sense of depth and enclosure. Sparse, twinkling stars might be visible through the canopy, adding to the magical atmosphere. The air feels cool and still, typical of a quiet night in the woods.",
			"lighting": {
			"conditions": "Cool moonlight",
			"direction": "Top-down, casting soft illumination on the owl's upper surfaces.",
			"shadows": "Soft, diffused shadows beneath the owl and within the deeper parts of the tree, indicating a gentle light source."
			},
			"aesthetics": {
			"composition": "Centered, with the owl as the focal point, framed by the tree branches.",
			"color_scheme": "Cool blues, silvers, and deep greens, contrasted with the earthy tones of the owl's feathers.",
			"mood_atmosphere": "Whimsical, enchanting, mysterious, serene, charming.",
			"preference_score": "very high",
			"aesthetic_score": "very high"
			},
			"photographic_characteristics": {
			"depth_of_field": "Shallow, with the owl in sharp focus and the background softly blurred.",
			"focus": "Sharp focus on the owl's eyes and the intricate details of its feathers.",
			"camera_angle": "Eye-level, creating an intimate connection with the owl.",
			"lens_focal_length": "Telephoto, to isolate the owl and create a compressed background."
			},
			"style_medium": "digital illustration",
			"context": "This is a concept for a whimsical digital illustration, possibly for a children's storybook cover or a fantasy art print, designed to evoke a sense of wonder and charm.",
			"artistic_style": "detailed, whimsical, enchanting"
		}
  },
	{
    imageUrl: 'http://labs-assets.bria.ai/A_clear_bottle_of_hair_serum_with_golden_liquid__surrounded_by_fresh_botanical_i.png',
    suggestedPrompt: 'A clear bottle of hair serum with golden liquid, surrounded by fresh botanical ingredients like argan nuts and leaves, bright natural sunlight — placed on a clean stone surface, soft diffused highlights, translucent reflections, warm golden tones, dewy freshness in the air, delicate shadows from leaves, premium organic aesthetic, serene and radiant atmosphere.',
		structured_prompt: {
			"short_description": "A pristine, clear glass bottle of hair serum, filled with a radiant golden liquid, stands elegantly at the center of the frame. It is artfully arranged amidst a scattering of fresh, vibrant botanical ingredients, including textured argan nuts and delicate green leaves, some still bearing tiny dewdrops. The scene is bathed in bright, natural sunlight, creating soft, diffused highlights and translucent reflections on the bottle and the smooth, clean stone surface it rests upon. The overall atmosphere is one of serene radiance, emphasizing a premium, organic aesthetic with warm golden tones and subtle, delicate shadows cast by the leaves.",
			"objects": [
			{
			"description": "A sleek, cylindrical clear glass bottle, meticulously clean and polished, containing a rich, viscous golden liquid. The liquid appears to shimmer and glow, suggesting a luxurious and potent serum. The bottle has a simple, elegant design with a subtle, reflective cap.",
			"location": "center",
			"relationship": "The primary subject, surrounded and complemented by the botanical elements.",
			"relative_size": "large within frame",
			"shape_and_color": "Cylindrical, clear glass with golden liquid",
			"texture": "Smooth, reflective glass",
			"appearance_details": "Translucent reflections and soft, diffused highlights catch the light, emphasizing the clarity of the glass and the richness of the serum. Tiny air bubbles might be visible within the liquid, adding to its natural appeal.",
			"orientation": "upright"
			},
			{
			"description": "A cluster of natural, unpolished argan nuts, varying slightly in size and shape, with their characteristic textured shells. Some may be partially cracked open to reveal the kernel within.",
			"location": "bottom-left foreground",
			"relationship": "Scattered around the base of the serum bottle, providing context for its ingredients.",
			"relative_size": "small",
			"shape_and_color": "Irregular oval, light to medium brown",
			"texture": "Rough, woody shell",
			"appearance_details": "Subtle variations in color and texture, some with faint dust or natural markings. A few might have a slight sheen from the ambient moisture.",
			"number_of_objects": 5,
			"orientation": "randomly scattered"
			},
			{
			"description": "Fresh, vibrant green leaves, likely from the argan tree or similar botanical source, with delicate veins and a slightly dewy surface. Some leaves might be curled or slightly overlapping.",
			"location": "bottom-right and mid-left, surrounding the bottle",
			"relationship": "Interspersed with the argan nuts and framing the serum bottle, enhancing the natural and organic theme.",
			"relative_size": "small to medium",
			"shape_and_color": "Oval to lanceolate, various shades of green",
			"texture": "Smooth with visible veins, some with tiny water droplets",
			"appearance_details": "Tiny, glistening dewdrops cling to the surface of some leaves, indicating freshness. Delicate shadows are cast by their edges onto the stone surface.",
			"number_of_objects": 7,
			"orientation": "randomly placed, some slightly curled"
			},
			{
			"description": "A single, larger argan nut, perhaps slightly more prominent than the others, positioned to draw the eye.",
			"location": "mid-right foreground",
			"relationship": "Part of the botanical arrangement, acting as a visual anchor on one side.",
			"relative_size": "medium",
			"shape_and_color": "Irregular oval, medium brown",
			"texture": "Rough, woody shell",
			"appearance_details": "Clearly defined texture, with a subtle highlight catching its curve.",
			"orientation": "lying on its side"
			}
			],
			"background_setting": "The background is a continuation of the clean, light-colored stone surface, extending into a soft, out-of-focus blur. This minimalist setting ensures that the focus remains entirely on the product and its natural ingredients. The air around the objects appears subtly humid and fresh, with a hint of dewy atmosphere.",
			"lighting": {
			"conditions": "Bright natural sunlight",
			"direction": "Top-down and slightly from the left, creating gentle modeling.",
			"shadows": "Delicate, soft-edged shadows are cast by the leaves and the bottle, extending subtly across the stone surface, adding depth without harshness."
			},
			"aesthetics": {
			"composition": "Centered with elements arranged in a loose, organic circle around the main subject, creating a sense of balance and natural flow.",
			"color_scheme": "Warm complementary colors, dominated by golden yellows and rich greens against a neutral, light grey stone, with subtle hints of brown.",
			"mood_atmosphere": "Serene, radiant, fresh, and luxurious, evoking a sense of natural purity and premium quality.",
			"preference_score": "very high",
			"aesthetic_score": "very high"
			},
			"photographic_characteristics": {
			"depth_of_field": "Shallow, with the serum bottle and immediate surrounding botanicals in sharp focus, and the background gently blurred.",
			"focus": "Sharp focus on the serum bottle and the closest botanical elements.",
			"camera_angle": "Slightly elevated eye-level, looking down onto the arrangement, providing a clear view of the top of the bottle and the scattered ingredients.",
			"lens_focal_length": "Standard to short telephoto, to compress the scene slightly and enhance the shallow depth of field."
			},
			"style_medium": "photograph",
			"context": "This is a concept for a high-end product photograph, ideal for a beauty brand's marketing campaign, website, or social media, emphasizing natural ingredients and a luxurious feel."
		}
  }
]

function PoweredByBanner() {
  return (
    <div className="bg-[#5300c9] text-white text-center text-base md:text-lg py-2">
      <a
        href="https://replicate.com/bria/fibo?utm_source=project&utm_campaign=kontext-chat"
        target="_blank"
        rel="noopener noreferrer"
        className="underline"
      >
        Powered by FIBO on Replicate
      </a>
    </div>
  );
}

function App() {
  // State for upload vs chat mode
  const [showUpload, setShowUpload] = React.useState(true);
  const [messages, setMessages] = React.useState([]);
  const [input, setInput] = React.useState('');
  const [loading, setLoading] = React.useState(false);
  // No need for separate image state - just use what's in the chat!
  const [predictionId, setPredictionId] = React.useState(null);
  const [abortController, setAbortController] = React.useState(null);
  // New: track if a starter image was used (for future logic if needed)
  const [starterUsed, setStarterUsed] = React.useState(false);

  // Replicate API token state
  const [replicateToken, setReplicateToken] = React.useState(() => {
    if (typeof window !== 'undefined') {
      return localStorage.getItem('replicateApiToken') || '';
    }
    return '';
  });
  const [showTokenModal, setShowTokenModal] = React.useState(!replicateToken);
  const [tokenInput, setTokenInput] = React.useState('');
  const [tokenError, setTokenError] = React.useState('');

  // Simple desktop check
  const [isDesktop, setIsDesktop] = React.useState(false);
  React.useEffect(() => {
    const checkDesktop = () => setIsDesktop(window.innerWidth >= 768);
    checkDesktop();
    window.addEventListener('resize', checkDesktop);
    return () => window.removeEventListener('resize', checkDesktop);
  }, []);

  // Ref for chat container and file input
  const chatContainerRef = React.useRef(null);
  const fileInputRef = React.useRef(null);
  const textareaRef = React.useRef(null);

  // Focus textarea when component mounts or when switching to chat mode
  React.useEffect(() => {
    if (!showUpload && textareaRef.current) {
      textareaRef.current.focus();
      textareaRef.current.select();
    }
  }, [showUpload]);

  // Scroll to bottom when messages change
  React.useEffect(() => {
    if (chatContainerRef.current) {
      setTimeout(() => {
        chatContainerRef.current.scrollTop = chatContainerRef.current.scrollHeight;
      }, 50);
    }
  }, [messages]);

  // Lock body scrolling on mobile for a consistent experience across screens
  React.useEffect(() => {
    if (!isDesktop) {
      document.body.style.overflow = 'hidden';
      document.body.style.position = 'fixed';
      document.body.style.width = '100%';
    } else {
      document.body.style.overflow = '';
      document.body.style.position = '';
      document.body.style.width = '';
    }

    return () => {
      document.body.style.overflow = '';
      document.body.style.position = '';
      document.body.style.width = '';
    };
  }, [isDesktop]);

  // Drag and drop state
  const [dragActive, setDragActive] = React.useState(false);

  // Helper to scroll to bottom (for image onLoad)
  function scrollToBottom() {
    if (chatContainerRef.current) {
      setTimeout(() => {
        chatContainerRef.current.scrollTop = chatContainerRef.current.scrollHeight;
      }, 50);
    }
  }

  // Drag and drop handlers for upload area
  function handleDrag(e) {
    e.preventDefault();
    e.stopPropagation();
    if (e.type === 'dragenter' || e.type === 'dragover') {
      setDragActive(true);
    } else if (e.type === 'dragleave') {
      setDragActive(false);
    }
  }

  function handleDrop(e) {
    e.preventDefault();
    e.stopPropagation();
    setDragActive(false);
    const files = e.dataTransfer.files;
    if (files && files[0] && files[0].type.startsWith('image/')) {
      handleFile(files[0]);
    }
  }

  // Handle file selection
  function handleFileSelect(e) {
    const file = e.target.files[0];
    if (file) {
      handleFile(file);
    }
  }

  // Process uploaded file
  async function handleFile(file) {
    if (!file.type.startsWith('image/')) {
      alert('Please select a valid image file.');
      return;
    }

    // Validate file size (10MB limit)
    const maxSize = 10 * 1024 * 1024;
    if (file.size > maxSize) {
      alert('Image file is too large. Please select an image under 10MB.');
      return;
    }

    try {
      // Scale down the image to 1 megapixel
      const scaledBlob = await scaleImageTo1Megapixel(file);
      const url = URL.createObjectURL(scaledBlob);

      // Add initial messages
      setMessages([
        { type: 'image', image: url, imageBlob: scaledBlob, from: 'assistant', id: Date.now() },
        { type: 'text', text: 'Image uploaded! How would you like to edit it?', from: 'system', id: Date.now() + 1 }
      ]);

      // Switch to chat mode
      setShowUpload(false);
    } catch (error) {
      alert('Failed to process image: ' + error.message);
    }
  }

  // Handle click on starter image
  async function handleStarterImageClick(starter) {
    // Fetch the image as a blob so it behaves like uploaded images
    try {
      setLoading(true);
      const res = await fetch(starter.imageUrl);
      const blob = await res.blob();
      // Add image as first message
      setMessages([
        { type: 'image', image: starter.imageUrl, imageBlob: blob, structuredPrompt: starter.structured_prompt, from: 'assistant', id: Date.now() },
        { type: 'text', text: "Image loaded! Tell me how you'd like to refine it.", from: 'system', id: Date.now() + 1 }
      ]);
      setShowUpload(false);
      setInput(starter.suggestedPrompt || '');
      setStarterUsed(true);
    } catch (err) {
      alert('Failed to load starter image.');
    } finally {
      setLoading(false);
    }
  }

  // Helper function to convert blob to data URL
  function blobToDataUrl(blob) {
    return new Promise((resolve, reject) => {
      const reader = new FileReader();
      reader.onload = () => resolve(reader.result);
      reader.onerror = reject;
      reader.readAsDataURL(blob);
    });
  }

  // Get the most recent image from the chat
  function getLastImageBlob() {
    for (let i = messages.length - 1; i >= 0; i--) {
      if (messages[i].type === 'image' && messages[i].imageBlob) {
        return messages[i].imageBlob;
      }
    }
    return null;
  }

	// Get the most recent structured prompt from the chat
  function getLastStructuredPrompt() {
    for (let i = messages.length - 1; i >= 0; i--) {
      if (messages[i].type === 'image' && messages[i].imageBlob) {
        return messages[i].structuredPrompt;
      }
    }
    return null;
  }

  // Scale image function
  async function scaleImageTo1Megapixel(file) {
    return new Promise((resolve, reject) => {
      const img = new Image();
      const canvas = document.createElement('canvas');
      const ctx = canvas.getContext('2d');

      img.onload = () => {
        const originalWidth = img.width;
        const originalHeight = img.height;
        const originalPixels = originalWidth * originalHeight;
        const targetPixels = 1000000;

        let newWidth, newHeight;

        if (originalPixels <= targetPixels) {
          newWidth = originalWidth;
          newHeight = originalHeight;
        } else {
          const scaleFactor = Math.sqrt(targetPixels / originalPixels);
          newWidth = Math.round(originalWidth * scaleFactor);
          newHeight = Math.round(originalHeight * scaleFactor);
        }

        canvas.width = newWidth;
        canvas.height = newHeight;
        ctx.drawImage(img, 0, 0, newWidth, newHeight);

        canvas.toBlob((blob) => {
          if (blob) {
            resolve(blob);
          } else {
            reject(new Error('Failed to create scaled image'));
          }
        }, 'image/jpeg', 0.9);
      };

      img.onerror = () => {
        reject(new Error('Failed to load image'));
      };

      img.src = URL.createObjectURL(file);
    });
  }

  // Reset to upload mode
  function resetApp() {
    if (abortController) {
      abortController.abort();
    }
    setShowUpload(true);
    setMessages([]);
    setInput('');
    setPredictionId(null);
    setAbortController(null);
    setLoading(false);
    setStarterUsed(false);
    if (fileInputRef.current) {
      fileInputRef.current.value = '';
    }
  }

  // Handle sending a message
  const handleSend = async (e) => {
    e.preventDefault();
		const lastStructuredPrompt = getLastStructuredPrompt()
    if (!input.trim() || loading || !lastStructuredPrompt || !replicateToken) return;

    const userMsg = { type: 'text', text: input, from: 'user', id: Date.now() };
    setMessages(prev => [...prev, userMsg]);
    setInput('');
    setLoading(true);

    // Add loading message
    const loadingMsg = { type: 'loading', from: 'assistant', id: Date.now() + 1 };
    setMessages(prev => [...prev, loadingMsg]);

    try {
      // Create abort controller for this request
      const controller = new AbortController();
      setAbortController(controller);


      console.log('Sending request to /generate-image...');
      const requestBody = {
        prompt: input,
        structured_prompt: lastStructuredPrompt
      };
      console.log('Request body size:', JSON.stringify(requestBody).length);

      const res = await fetch('/generate-image', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'X-Replicate-Api-Token': replicateToken },
        body: JSON.stringify(requestBody),
        signal: controller.signal
      });

      console.log('Response status:', res.status);
      console.log('Response headers:', Object.fromEntries(res.headers.entries()));

      if (!res.ok) {
        const errorText = await res.text();
        console.error('Error response body:', errorText);
        throw new Error(`HTTP ${res.status}: ${errorText}`);
      }

      // The response is now JSON with the Cloudflare Images URL
      const result = await res.json();
      console.log('Response result:', result);

      if (result.error) {
        throw new Error(result.error);
      }

      const imageUrl = result.imageUrl;
      console.log('Cloudflare Images URL:', imageUrl);

      // Fetch the image to create a blob for local storage/UI purposes
      const imageResponse = await fetch(imageUrl);
      const imageBlob = await imageResponse.blob();
      console.log('Image blob size:', imageBlob.size);

      // Replace loading with image (store blob in message)
      setMessages(prev => prev.map(msg =>
        msg.type === 'loading' ?
          { type: 'image', image: imageUrl, imageBlob: imageBlob, structuredPrompt: lastStructuredPrompt, from: 'assistant', id: msg.id } :
          msg
      ));

      // Add delete button to user message
      setMessages(prev => prev.map(msg =>
        msg.id === userMsg.id ? { ...msg, showDelete: true } : msg
      ));

    } catch (err) {
      // Don't show error if request was aborted (cancelled)
      if (err.name !== 'AbortError') {
        setMessages(prev => prev.filter(msg => msg.type !== 'loading'));
        setMessages(prev => [...prev, {
          type: 'text',
          text: 'Sorry, there was an error generating the image: ' + err.message,
          from: 'assistant',
          id: Date.now()
        }]);
      } else {
        console.log('Request was cancelled');
        setMessages(prev => prev.filter(msg => msg.type !== 'loading'));
      }
    } finally {
      setLoading(false);
      setPredictionId(null);
      setAbortController(null);
    }
  };

  // Cancel generation
  function cancelGeneration() {
    console.log('Cancel generation called');

    // Abort the ongoing request
    if (abortController) {
      console.log('Aborting request...');
      abortController.abort();
      setAbortController(null);
    }

    // Stop loading
    setLoading(false);
    setPredictionId(null);

    // Find the most recent user message to restore to input
    const currentMessages = [...messages];
    const lastUserMessage = currentMessages.slice().reverse().find(msg => msg.from === 'user' && msg.type === 'text');
    console.log('Last user message:', lastUserMessage);

    // Remove loading message and the most recent user message
    // This automatically makes the previous image the "last image" again
    setMessages(prev => {
      const filtered = prev.filter(msg =>
        msg.type !== 'loading' &&
        !(lastUserMessage && msg.from === 'user' && msg.type === 'text' && msg.id === lastUserMessage.id)
      );
      console.log('Messages after cancel:', filtered.length);
      return filtered;
    });

    // Restore the cancelled message to input
    if (lastUserMessage) {
      setTimeout(() => {
        console.log('Restoring text to input:', lastUserMessage.text);
        setInput(lastUserMessage.text);
      }, 50);
    }
  }

  // Delete message and all subsequent messages
  function deleteFromMessage(messageId) {
    const messageIndex = messages.findIndex(msg => msg.id === messageId);
    if (messageIndex === -1) return;

    // Keep messages up to the clicked message (excluding it)
    const remainingMessages = messages.slice(0, messageIndex);
    setMessages(remainingMessages);

    // No need to set current image - getLastImageBlob will find it automatically
  }

  // No longer needed - state is derived from chat messages

  // Handle image click for full screen
  function handleImageClick(imageUrl) {
    window.open(imageUrl, '_blank');
  }

  // Attach drag events when in upload mode
  React.useEffect(() => {
    if (showUpload) {
      window.addEventListener('dragenter', handleDrag);
      window.addEventListener('dragover', handleDrag);
      window.addEventListener('dragleave', handleDrag);
      window.addEventListener('drop', handleDrop);
      return () => {
        window.removeEventListener('dragenter', handleDrag);
        window.removeEventListener('dragover', handleDrag);
        window.removeEventListener('dragleave', handleDrag);
        window.removeEventListener('drop', handleDrop);
      };
    }
  }, [showUpload]);

  React.useEffect(() => {
    if (!replicateToken) {
      setShowTokenModal(true);
    } else {
      setShowTokenModal(false);
    }
  }, [replicateToken]);

  function handleTokenSubmit(e) {
    e.preventDefault();
    if (!tokenInput.trim()) {
      setTokenError('Please enter your Replicate API token.');
      return;
    }
    localStorage.setItem('replicateApiToken', tokenInput.trim());
    setReplicateToken(tokenInput.trim());
    setShowTokenModal(false);
    setTokenInput('');
    setTokenError('');
  }

  function handleTokenLogout() {
    localStorage.removeItem('replicateApiToken');
    setReplicateToken('');
    setShowTokenModal(true);
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-[#5300c9] to-[#8800ff] md:overflow-auto overflow-hidden">
      {/* Replicate API Token Modal */}
      {showTokenModal && (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-60">
          <div className="bg-white rounded-2xl shadow-xl p-8 max-w-md w-full flex flex-col items-center">
            <img src="/bria-logo.svg" className="w-1/3 mx-auto mb-4" alt="Bria" />
            <h2 className="text-xl font-bold mb-2 text-center">Enter your Replicate API Token</h2>
            <p className="text-gray-700 text-center mb-4">To use Kontext Chat, you'll need a Replicate API token.<br />
              <a href="https://replicate.com/account/api-tokens?new-token-name=kontext-chat" target="_blank" rel="noopener noreferrer" className="underline text-[#5300c9]">Create a token here</a> and paste it below.
            </p>
            <form onSubmit={handleTokenSubmit} className="w-full flex flex-col items-center">
              <input
                type="text"
                className="w-full border border-gray-300 rounded-lg px-4 py-2 mb-2 text-base"
                placeholder="Paste your Replicate API token"
                value={tokenInput}
                onChange={e => setTokenInput(e.target.value)}
                autoFocus
              />
              {tokenError && <div className="text-red-600 text-sm mb-2">{tokenError}</div>}
              <button
                type="submit"
                className="w-full bg-[#5300c9] hover:bg-[#3f00a0] text-white rounded-lg px-4 py-2 font-semibold transition-colors"
              >
                Save Token
              </button>
            </form>
          </div>
        </div>
      )}
      {/* Main Content */}
      <div className="min-h-screen flex flex-col md:flex md:items-center h-screen md:h-auto">
        {showUpload ? (
          /* Upload Section */
          <div className="w-full md:max-w-4xl bg-white md:shadow-md flex flex-col h-screen md:h-auto overflow-hidden md:overflow-visible">
            {/* Logo */}
            <div className="p-4 md:p-2 bg-white border-b border-gray-200">
              <img src="/bria-logo.svg" className="w-1/3 md:w-1/4 mx-auto" alt="Bria" />
            </div>
            <PoweredByBanner />

            {/* Upload Area */}
            <div className="flex-1 flex flex-col p-4 md:p-6 overflow-y-auto md:overflow-visible pb-32 md:pb-6" style={{paddingBottom: 'calc(8rem + env(safe-area-inset-bottom))'}}>
              {/* Intro Text */}
              <div className="text-center mb-6">
                <p className="text-gray-700 text-base md:text-lg">
                  Chat with images to edit them.
                </p>
              </div>

              <div
                className={`border-2 border-dashed rounded-2xl p-6 md:p-12 text-center cursor-pointer mb-12 ${
                  dragActive
                    ? 'border-green-400 bg-green-50 text-green-700'
                    : 'border-gray-300 bg-gray-50 hover:border-[#5300c9] hover:bg-purple-50 text-gray-700 hover:text-[#5300c9]'
                }`}
                onClick={() => fileInputRef.current?.click()}
              >
                <div className="upload-content">
                  <svg className="w-10 h-10 md:w-12 md:h-12 mx-auto mb-3 md:mb-4 opacity-70" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"></path>
                  </svg>
                  <h3 className="text-lg md:text-xl mb-1 md:mb-2 font-semibold">Upload an image to get started</h3>
                  <p className="text-base md:text-lg opacity-80">Drag and drop an image here, or click to browse</p>
                  <input
                    type="file"
                    ref={fileInputRef}
                    accept="image/*"
                    className="hidden"
                    onChange={handleFileSelect}
                  />
                </div>
              </div>

              {/* Starter Images Section */}
              <div className="text-center text-gray-600 text-base mb-4 font-medium">Or choose a starting image:</div>
              <div className="grid grid-cols-2 md:grid-cols-5 gap-3">
                {starterImages.map((starter, idx) => (
                  <button
                    key={idx}
                    className="aspect-square w-full rounded-xl overflow-hidden border-2 border-gray-200 hover:border-orange-400 focus:border-orange-500 transition-all shadow-sm bg-gray-50 group"
                    onClick={() => handleStarterImageClick(starter)}
                    disabled={loading}
                    title={starter.suggestedPrompt}
                  >
                    <img
                      src={starter.imageUrl}
                      alt={starter.suggestedPrompt}
                      className="object-cover w-full h-full group-hover:scale-105 transition-transform duration-200"
                    />
                  </button>
                ))}
              </div>

              {/* Footer Text */}
              <div className="mt-8 pt-6 border-t border-gray-200">
                <p className="text-gray-600 text-base md:text-lg leading-relaxed text-center">
                  FIBO Chat is powered by <a href="https://replicate.com/bria/fibo?utm_source=project&utm_campaign=kontext-chat" className="underline text-[#5300c9] hover:text-[#3f00a0]">FIBO</a> on <a href="https://replicate.com?utm_source=project&utm_campaign=kontext-chat" className="underline text-[#5300c9] hover:text-[#3f00a0]">Replicate</a>.
                </p>
              </div>
            </div>
          </div>
        ) : (
          /* Chat Section */
          <div className="w-full md:max-w-4xl bg-white md:shadow-md overflow-hidden flex flex-col h-screen md:h-screen relative">
            {/* Chat Header with Logo */}
            <div className="p-4 md:p-2 bg-white border-b border-gray-200 relative flex items-center flex-shrink-0">
              <button
                onClick={resetApp}
                className="absolute left-4 w-8 h-8 bg-[#5300c9] hover:bg-[#3f00a0] text-white rounded-full flex items-center justify-center transition-all duration-200 hover:scale-105"
                title="Back to upload"
              >
                <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                </svg>
              </button>
              <img
                src="/bria-logo.svg"
                className="w-1/3 md:w-1/4 mx-auto cursor-pointer hover:opacity-90 transition-opacity"
                alt="Bria"
                onClick={resetApp}
                title="Back to upload"
              />
              {/* Token logout button */}
              {replicateToken && (
                <button
                  onClick={handleTokenLogout}
                  className="absolute right-4 w-8 h-8 bg-gray-200 hover:bg-red-500 text-gray-700 hover:text-white rounded-full flex items-center justify-center transition-all duration-200"
                  title="Remove Replicate API Token"
                >
                  <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M6 18L18 6M6 6l12 12"></path>
                  </svg>
                </button>
              )}
            </div>
            <PoweredByBanner />

            {/* Chat Messages */}
            <div className="flex-1 overflow-y-auto p-6 space-y-4 pb-56 md:pb-6" ref={chatContainerRef}>
              {messages.map((msg) => (
                <div
                  key={msg.id}
                  className={`flex ${msg.from === 'user' ? 'justify-end' : 'justify-start'}`}
                >
                  <div
                    className={`relative max-w-sm md:max-w-md ${
                      msg.from === 'user'
                        ? 'bg-gradient-to-br from-blue-500 to-purple-600 text-white rounded-3xl px-4 py-3'
                        : msg.from === 'system'
                        ? 'bg-blue-50 text-blue-800 rounded-2xl px-4 py-3 italic'
                        : 'bg-gray-100 text-gray-800 rounded-2xl rounded-bl-md px-4 py-3'
                    }`}
                  >
                    {msg.type === 'image' && (
                      <img
                        src={msg.image}
                        alt="Generated image"
                        className="max-w-xs rounded-lg cursor-pointer hover:opacity-90 transition-opacity"
                        onClick={() => handleImageClick(msg.image)}
                        onLoad={scrollToBottom}
                      />
                    )}
                    {msg.type === 'loading' && (
                      <div className="flex flex-col items-center gap-4 py-8 px-12">
                        <div className="w-16 h-16 border-4 border-gray-300 border-t-[#5300c9] rounded-full animate-spin"></div>
                        <span className="text-gray-600">Generating image...</span>
                      </div>
                    )}
                    {msg.text && <div className="text-base md:text-lg">{msg.text}</div>}

                    {/* Delete button for user messages */}
                    {msg.showDelete && (
                      <button
                        onClick={() => deleteFromMessage(msg.id)}
                        className="absolute -top-2 -right-2 w-6 h-6 bg-red-500 hover:bg-red-600 text-white rounded-full flex items-center justify-center transition-all duration-200 hover:scale-110"
                        title="Delete from here and continue editing"
                      >
                        <svg className="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                        </svg>
                      </button>
                    )}
                  </div>
                </div>
              ))}
            </div>

            {/* Input Area */}
            <div className="fixed bottom-0 left-0 right-0 bg-white border-t p-4 md:relative md:border-t" style={{paddingBottom: 'calc(1rem + env(safe-area-inset-bottom))'}}>
              <form onSubmit={handleSend} className="flex items-end gap-3 max-w-4xl mx-auto">
                <div className="flex-1 relative">
                  <div className="bg-gray-50 rounded-3xl px-4 py-3 pr-12 border-2 border-transparent focus-within:border-[#5300c9] transition-colors">
                    <textarea
                      ref={textareaRef}
                      value={input}
                      onChange={(e) => setInput(e.target.value)}
                      placeholder="Describe your edit..."
                      className="w-full bg-transparent border-none outline-none resize-none text-base"
                      rows="1"
                      style={{ minHeight: '24px', maxHeight: '120px' }}
                      disabled={loading}
                      onKeyDown={(e) => {
                        if (e.key === 'Enter' && !e.shiftKey) {
                          e.preventDefault();
                          if (!loading && input.trim()) {
                            handleSend(e);
                          }
                        }
                      }}
                    />

                    {/* Send/Cancel Button */}
                    {loading ? (
                      <button
                        type="button"
                        onClick={cancelGeneration}
                        className="absolute right-2 top-1/2 transform -translate-y-1/2 w-8 h-8 bg-red-500 hover:bg-red-600 text-white rounded-full flex items-center justify-center transition-all duration-200"
                      >
                        <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                      </button>
                    ) : (
                      <button
                        type="submit"
                        disabled={!input.trim() || loading}
                        className="absolute right-2 top-1/2 transform -translate-y-1/2 w-8 h-8 bg-[#5300c9] hover:bg-[#3f00a0] disabled:bg-gray-300 text-white rounded-full flex items-center justify-center transition-all duration-200 disabled:cursor-not-allowed"
                      >
                        <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
                        </svg>
                      </button>
                    )}
                  </div>
                </div>
              </form>
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
